<%#= form_with(model: memo) do |form| %>
  <%# if memo.errors.any? %>
<!--    <div style="color: red">
      <h2><%#= pluralize(memo.errors.count, "error") %> prohibited this memo from being saved:</h2>

      <ul>
        <%# memo.errors.each do |error| %>-->
<!--          <li><%#= error.full_message %></li>-->
        <%# end %>
<!--      </ul>
    </div>
  <%# end %>-->
<!--
  <div>
    <%#= form.label :user_id, style: "display: block" %>-->
    <%#= form.text_field :user_id %>
<!--  </div>

  <div>
    <%#= form.label :memo_category_id, style: "display: block" %>-->
    <%#= form.text_field :memo_category_id %>
<!--  </div>

  <div>
    <%#= form.label :status, style: "display: block" %>-->
    <%#= form.number_field :status %>
<!--  </div>

  <div>
    <%#= form.label :description, style: "display: block" %>-->
    <%#= form.text_area :description %>
<!--  </div>

  <div>
    <%#= form.submit %>-->
<!--  </div>-->
<%# end %>
<!---->

<%= simple_form_for(@memo, html: { class: 'form-horizontal' }) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="card">
    <div class="card-body">
      <div class="form-group row">
        <%= f.label :memo_category, 'Select Category', class: 'col-sm-2 col-form-label' %>
        <div class="col-sm-10">
          <%= f.collection_select :memo_category_id, MemoCategory.all, :id, :name, { prompt: 'Choose a Category' }, class: 'form-control' %>
          <%= f.error :memo_category, class: 'text-danger' %>
        </div>
      </div>
      <div class="form-group row">
        <%= f.label :description, class: 'col-sm-2 col-form-label' %>
        <div class="col-sm-10">
          <%= f.rich_text_area :description, class: 'form-control', placeholder: 'Enter Description' %>
          <%= f.error :description, class: 'text-danger' %>
        </div>
      </div>
      <div class="form-group row">
        <%= f.label :amount, class: 'col-sm-2 col-form-label' %>
        <div class="col-sm-10">
          <%= f.number_field :amount, class: 'form-control', placeholder: 'Enter Amount', value: @memo.amount.nil? ? params.dig(:memo, :amount) || 0 : @memo.amount.to_i %>
          <%= f.error :amount, class: 'text-danger' %>
        </div>
      </div>
      <div class="form-group row">
        <%= f.label :memo_proof, class: 'col-sm-2 col-form-label' %>
        <div class="col-sm-10">
          <%= f.input :memo_proof, as: :file, class: 'form-control', label: false %>
          <%= f.error :memo_proof, class: 'text-danger' %>
          <br>
          <% if @memo.memo_proof? %>
            <%= image_tag @memo.memo_proof.url, alt: 'Proof Image', size: 128 %>
          <% else %>
            <p class="text-secondary">No file uploaded</p>
          <% end %>
        </div>
      </div>
      <br>
      <div class="form-group row">
        <div class="col-sm-10 offset-sm-2">
          <%= f.button :submit, class: 'btn btn-primary' %>
        </div>
      </div>
    </div>
  </div>
<% end %>