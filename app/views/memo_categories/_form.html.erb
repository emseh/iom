<%= form_with(model: memo_category) do |form| %>
  <% if memo_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(memo_category.errors.count, "error") %> prohibited this memo_category from being saved:</h2>

      <ul>
        <% memo_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
